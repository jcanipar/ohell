<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :numPlay %><br>
    <%= f.number_field :numPlay %>
  </div>
  <div class="field">
    <%= f.label :league %><br>
    <%= f.check_box :league %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
